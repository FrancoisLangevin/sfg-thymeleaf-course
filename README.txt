# Lien pour changer le local 
localhost:8080/?lang=fr
localhost:8080/?lang=ru
localhost:8080/?lang=en_US
localhost:8080/?lang=en_GB

# Section 29
On voit comment appeler des fragment de page pour éviter de dupliquer le code
Dans la page de fragment on precise le nom avec ceci: <div class="jumbotron" th:fragment="jumbotron">
On peut avoir plusieurs fragments dans une meme page avec des noms different

# Section 42
Fragment with iteration !!!!

Faire des pages statics et par la suite remplacer par les tags remove vrai data etc..
